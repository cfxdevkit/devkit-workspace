# build outputs
**/node_modules
**/dist
**/out
**/.turbo

# EXCEPTION: root dist dir is where the packaged .vsix lands — keep it empty
# (Docker will create it; the build step fills it)

# git
.git
.gitignore

# os
.DS_Store
Thumbs.db

# editor
.vscode
*.vsix
