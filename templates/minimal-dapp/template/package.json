{
	"name": "{{PACKAGE_NAME}}",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"deps:install": "pnpm install",
		"doctor": "node -e \"console.log('ok')\"",
		"doctor:json": "echo '{\"ok\":true}'",
		"clean": "rm -rf node_modules dapp/node_modules dapp/dist",
		"deploy": "echo 'No contracts in minimal-dapp template'",
		"dev": "pnpm --dir dapp dev",
		"serve": "pnpm --dir dapp serve",
		"build": "pnpm --dir dapp serve",
		"stack:up": "echo 'No docker stack in minimal-dapp template'",
		"stack:down": "echo 'No docker stack in minimal-dapp template'",
		"stack:rebuild": "echo 'No docker stack in minimal-dapp template'",
		"stack:status": "echo 'No docker stack in minimal-dapp template'",
		"stack:logs": "echo 'No docker stack in minimal-dapp template'"
	},
	"engines": {
		"node": ">=22",
		"pnpm": ">=9"
	}
}
