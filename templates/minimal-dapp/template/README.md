# {{PROJECT_NAME}}

This project was generated by `devkit` from the `minimal-dapp` template.

## Target

- target: `{{TARGET_NAME}}`
- base URL support: `{{BASE_URL_ENABLED}}`
- proxy support: `{{PROXY_ENABLED}}`
- code-server support: `{{CODE_SERVER_ENABLED}}`

## Commands

- `pnpm install` — install workspace dependencies
- `pnpm dev` — start the dapp dev server
- `pnpm serve` — alias for `pnpm dev`
