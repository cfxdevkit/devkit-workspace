# Network resilience — needed for rootless Podman (pasta) build environments.
# Increases retry count and timeouts so pnpm survives transient ECONNRESET.
fetch-retries=5
fetch-retry-mintimeout=15000
fetch-retry-maxtimeout=60000
fetch-timeout=120000
